# Development-only overrides. Never activate this profile outside a developer machine:
# it exposes the H2 console and logs every statement (including bound parameters).
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG
