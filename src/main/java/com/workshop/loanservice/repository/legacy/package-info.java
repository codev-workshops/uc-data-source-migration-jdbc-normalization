/**
 * Repositories over the legacy CDW tables, bound to {@code legacyEntityManagerFactory}.
 *
 * <p>Superseded by {@code com.workshop.loanservice.repository.modern}. These are
 * retained only for the migration source and for
 * {@code loanservice.datasource.mode=legacy}; do not add new features against them.
 */
package com.workshop.loanservice.repository.legacy;
